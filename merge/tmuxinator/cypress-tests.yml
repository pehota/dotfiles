# ~/.tmuxinator/cypress-tests.yml

name: cypress-tests
root: ~/Work/Shore/git/integration-test/

tmux_options: -f ~/.tmux.conf
windows:
  - editor: nvim
  - tools:
      layout: tiled
      panes:
        - git ls && git st
